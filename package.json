{
  "name": "railway-map",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "seed": "bun run --cwd apps/server seed",
    "init": "bash scripts/init.sh"
  },
  "devDependencies": {
    "turbo": "^2.3.0"
  }
}
